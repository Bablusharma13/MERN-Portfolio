# Required for the "Ask AI about me" chat widget.
# Get a free key at https://aistudio.google.com/apikey and put it in a .env.local file
# (never commit real keys — .env.local is already git-ignored).
GEMINI_API_KEY=
